"""
Load the eval dataset and vectorize it (in the same place where the documents from the test_rag.py scrip go).
This should be ran at least once, before being able to run the ragas_eval.py script. (You should run it if data/ doesn't
have the embeddings of this ragas test.json dataset.)

"""